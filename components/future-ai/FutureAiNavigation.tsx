import type { KeyboardEvent } from "react";
import type { FutureAiTab } from "@/lib/future-ai/demo";

export const futureAiTabs: { id: FutureAiTab; label: string }[] = [{ id: "professor", label: "Professor IA" }, { id: "capabilities", label: "Capacidades futuras" }, { id: "architecture", label: "Como funcionará" }, { id: "roadmap", label: "Roadmap" }];
export function FutureAiNavigation({ active, onChange }: { active: FutureAiTab; onChange: (tab: FutureAiTab) => void }) {
  const onKeyDown = (event: KeyboardEvent<HTMLDivElement>) => { if (!["ArrowLeft", "ArrowRight", "Home", "End"].includes(event.key)) return; event.preventDefault(); const current = futureAiTabs.findIndex((tab) => tab.id === active); const index = event.key === "Home" ? 0 : event.key === "End" ? futureAiTabs.length - 1 : event.key === "ArrowRight" ? (current + 1) % futureAiTabs.length : (current - 1 + futureAiTabs.length) % futureAiTabs.length; onChange(futureAiTabs[index].id); requestAnimationFrame(() => document.getElementById(`future-ai-tab-${futureAiTabs[index].id}`)?.focus()); };
  return <div role="tablist" aria-label="Áreas de Futura IA" onKeyDown={onKeyDown} className="flex max-w-full gap-1 overflow-x-auto rounded-xl border border-line bg-white p-1">{futureAiTabs.map((tab) => <button key={tab.id} id={`future-ai-tab-${tab.id}`} role="tab" type="button" aria-selected={active === tab.id} aria-controls={`future-ai-panel-${tab.id}`} tabIndex={active === tab.id ? 0 : -1} onClick={() => onChange(tab.id)} className={`min-h-11 shrink-0 rounded-lg px-4 py-2.5 text-sm font-semibold focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-focus ${active === tab.id ? "bg-neutral-950 text-white" : "text-neutral-700 hover:bg-neutral-100"}`}>{tab.label}</button>)}</div>;
}
