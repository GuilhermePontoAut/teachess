import type { FutureAiInteraction, SimulatedAnswerContent } from "@/lib/future-ai/demo";
const Section = ({ title, children }: { title: string; children: React.ReactNode }) => <section><h4 className="text-sm font-bold text-neutral-950">{title}</h4><div className="mt-1 text-sm leading-6 text-neutral-700">{children}</div></section>;
export function SimulatedAnswer({ answer }: { answer: SimulatedAnswerContent }) { return <div className="space-y-4"><Section title="Resumo simulado"><p>{answer.summary}</p></Section><Section title="O que observar"><ul className="list-disc space-y-1 pl-5">{answer.observations.map((item) => <li key={item}>{item}</li>)}</ul></Section><Section title="Principal aprendizado"><p>{answer.learning}</p></Section><Section title="Plano sugerido"><ol className="list-decimal space-y-1 pl-5">{answer.plan.map((item) => <li key={item}>{item}</li>)}</ol></Section><Section title="Próxima pergunta recomendada"><p>{answer.nextQuestion}</p></Section><Section title="Contexto utilizado"><p>{answer.contextUsed}</p></Section></div>; }
export function SimulatedConversation({ interactions }: { interactions: FutureAiInteraction[] }) { if (!interactions.length) return <div className="rounded-xl border border-dashed border-line-strong bg-neutral-50 px-5 py-10 text-center"><h3 className="font-semibold">Conversa vazia</h3><p className="mt-2 text-sm text-muted">Escolha um contexto e faça uma pergunta para criar a primeira resposta local.</p></div>; return <ol aria-label="Histórico da conversa simulada" className="space-y-5">{interactions.map((item) => <li id={`interaction-${item.id}`} key={item.id} className="scroll-mt-24 space-y-3"><article aria-label={`Pergunta: ${item.question}`} className="ml-auto max-w-[92%] rounded-2xl rounded-br-sm bg-neutral-950 p-4 text-white"><p className="text-xs font-bold uppercase tracking-wider text-neutral-400">Você · {item.context.legacy ? "Contexto antigo da demonstração" : item.context.label}</p><p className="mt-1 text-sm leading-6">{item.question}</p></article><article aria-label="Resposta simulada" className="max-w-[96%] rounded-2xl rounded-bl-sm border border-line bg-white p-5"><div className="mb-4 flex flex-wrap items-center justify-between gap-2"><span className="rounded-full bg-neutral-950 px-2.5 py-1 text-xs font-bold text-white">Simulado</span><time className="text-xs text-muted" dateTime={item.createdAt}>{new Date(item.createdAt).toLocaleTimeString("pt-BR", { hour: "2-digit", minute: "2-digit" })}</time></div><SimulatedAnswer answer={item.answer} /></article></li>)}</ol>; }
