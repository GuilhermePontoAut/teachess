"use client";
import { RotateCcw } from "lucide-react";
import { useEffect, useRef } from "react";
export function NewConversationDialog({ open, onCancel, onConfirm }: { open: boolean; onCancel: () => void; onConfirm: () => void }) { const ref = useRef<HTMLDialogElement>(null); useEffect(() => { const dialog = ref.current; if (open && !dialog?.open) dialog?.showModal(); if (!open && dialog?.open) dialog.close(); }, [open]); return <dialog ref={ref} onCancel={(event) => { event.preventDefault(); onCancel(); }} onClose={onCancel} aria-labelledby="new-conversation-title" aria-describedby="new-conversation-description" className="m-auto w-[calc(100%-2rem)] max-w-md rounded-2xl border border-line bg-white p-0 shadow-2xl backdrop:bg-black/60"><div className="p-6"><span className="flex size-11 items-center justify-center rounded-full bg-neutral-100"><RotateCcw size={20} aria-hidden="true" /></span><h2 id="new-conversation-title" className="mt-4 text-lg font-semibold">Iniciar nova conversa?</h2><p id="new-conversation-description" className="mt-2 text-sm leading-6 text-muted">Somente o histórico desta demonstração será apagado. Partidas, análises, posições e treinamento serão preservados.</p></div><div className="flex flex-col-reverse gap-2 border-t border-line bg-neutral-50 p-4 sm:flex-row sm:justify-end"><button type="button" onClick={onCancel} className="rounded-xl border border-line bg-white px-4 py-2.5 text-sm font-semibold">Cancelar</button><button type="button" onClick={onConfirm} className="rounded-xl bg-neutral-950 px-4 py-2.5 text-sm font-semibold text-white">Limpar histórico</button></div></dialog>; }
