import { CheckCircle2, Play, RotateCcw } from "lucide-react";
import type { ExerciseProgress, TrainingExercise } from "./training";
import { categoryLabels, levelLabels } from "./training";

export function ExerciseCard({ exercise, progress, onOpen, onReset }: { exercise: TrainingExercise; progress: ExerciseProgress; onOpen: () => void; onReset: () => void }) {
  const statusLabel = progress.status === "completed" ? "Concluído" : progress.status === "in_progress" ? "Em andamento" : "Não iniciado";
  return <article className="rounded-2xl border border-line bg-white p-5 shadow-sm"><div className="flex flex-wrap gap-2"><span className="rounded-full bg-neutral-100 px-2.5 py-1 text-xs font-semibold">{categoryLabels[exercise.category]}</span><span className="rounded-full border border-line px-2.5 py-1 text-xs font-semibold">{levelLabels[exercise.difficulty]}</span><span className="rounded-full border border-line px-2.5 py-1 text-xs font-semibold">{statusLabel}</span></div><h3 className="mt-3 text-lg font-semibold">{exercise.title}</h3><p className="mt-2 text-sm leading-6 text-muted">{exercise.description}</p><p className="mt-3 text-xs text-muted">{exercise.questionCount} questões simuladas · {exercise.estimatedMinutes} min</p><div className="mt-4"><div className="flex justify-between text-xs"><span>Progresso</span><strong>{progress.progress}%</strong></div><div className="mt-1 h-2 overflow-hidden rounded-full bg-neutral-200" role="progressbar" aria-label={`Progresso de ${exercise.title}`} aria-valuenow={progress.progress} aria-valuemin={0} aria-valuemax={100}><div className="h-full bg-green-700" style={{ width: `${progress.progress}%` }} /></div></div><p className="mt-3 text-xs leading-5 text-muted">Origem: {exercise.recommendationOrigin}</p><div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={onOpen} className="inline-flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-neutral-950 px-3 py-2 text-sm font-semibold text-white">{progress.status === "completed" ? <CheckCircle2 size={16} /> : <Play size={16} />}{progress.status === "not_started" ? "Iniciar exercício" : progress.status === "in_progress" ? "Continuar" : "Ver conclusão"}</button>{progress.status !== "not_started" && <button type="button" onClick={onReset} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-line px-3 py-2 text-sm font-semibold"><RotateCcw size={16} />Reiniciar</button>}</div></article>;
}
