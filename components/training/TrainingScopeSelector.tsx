export function TrainingScopeSelector({ includeExternal, onChange }: { includeExternal: boolean; onChange: (value: boolean) => void }) {
  return <section aria-labelledby="training-scope-title" className="rounded-2xl border border-line bg-surface p-5 shadow-sm"><h2 id="training-scope-title" className="font-semibold">Partidas usadas no treinamento</h2><div className="mt-3 grid gap-2 sm:grid-cols-2" role="radiogroup" aria-label="Escopo das partidas">{[[false, "Somente partidas da plataforma"], [true, "Incluir partidas externas"]].map(([value, label]) => <label key={String(value)} className={`flex cursor-pointer items-center gap-3 rounded-xl border p-3 text-sm font-semibold ${includeExternal === value ? "border-neutral-900 bg-neutral-100" : "border-line"}`}><input type="radio" name="training-scope" checked={includeExternal === value} onChange={() => onChange(Boolean(value))} className="size-4 accent-neutral-950" />{label}</label>)}</div>{includeExternal && <p className="mt-4 rounded-xl border border-blue-200 bg-blue-50 px-4 py-3 text-sm leading-6 text-blue-900">As partidas externas podem contribuir para seu treinamento privado, mas não interferem no ranking nem nas estatísticas oficiais.</p>}</section>;
}
