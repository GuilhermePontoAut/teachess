import { RotateCcw } from "lucide-react";
import { useEffect, useRef } from "react";
export function ResetTrainingDialog({ open, onCancel, onConfirm }: { open: boolean; onCancel: () => void; onConfirm: () => void }) { const ref = useRef<HTMLDialogElement>(null); useEffect(() => { const dialog = ref.current; if (open && !dialog?.open) dialog?.showModal(); if (!open && dialog?.open) dialog.close(); }, [open]); return <dialog ref={ref} onCancel={(event) => { event.preventDefault(); onCancel(); }} onClose={onCancel} aria-labelledby="reset-training-title" aria-describedby="reset-training-description" className="m-auto w-[calc(100%-2rem)] max-w-md rounded-2xl border border-line bg-white p-0 shadow-2xl backdrop:bg-black/60"><div className="p-6"><span className="flex size-11 items-center justify-center rounded-full bg-neutral-100"><RotateCcw size={20} /></span><h2 id="reset-training-title" className="mt-4 text-lg font-semibold">Restaurar plano de demonstração?</h2><p id="reset-training-description" className="mt-2 text-sm leading-6 text-muted">Atividades adicionadas, conclusões e progresso dos exercícios serão substituídos pelo estado inicial simulado.</p></div><div className="flex flex-col-reverse gap-2 border-t border-line bg-neutral-50 p-4 sm:flex-row sm:justify-end"><button type="button" onClick={onCancel} className="rounded-xl border border-line bg-white px-4 py-2.5 text-sm font-semibold">Cancelar</button><button type="button" onClick={onConfirm} className="rounded-xl bg-neutral-950 px-4 py-2.5 text-sm font-semibold text-white">Restaurar plano</button></div></dialog>; }
