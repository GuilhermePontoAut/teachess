import { BadgeCheck, CalendarDays, Clock, MapPin, Star } from "lucide-react";
import type { HumanTeacher } from "@/lib/types/humanTeachers";
import { currency, formatLabel, nextSlot } from "./humanTeachers";
export function TeacherCard({ teacher, onProfile, onBooking }: { teacher: HumanTeacher; onProfile: () => void; onBooking: () => void }) { const slot = nextSlot(teacher); return <article className="flex h-full flex-col rounded-2xl border border-line bg-white p-5 shadow-sm"><div className="flex items-start gap-3"><span aria-hidden="true" className="flex size-12 shrink-0 items-center justify-center rounded-full bg-neutral-950 font-bold text-white">{teacher.initials}</span><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="font-semibold">{teacher.name}</h2>{teacher.verified && <BadgeCheck size={17} aria-label="Verificação simulada"/>}</div><p className="mt-1 flex items-center gap-1 text-xs text-muted"><MapPin size={13}/>{teacher.region}</p></div></div><div className="mt-4 flex flex-wrap gap-2"><span className="rounded-full bg-neutral-950 px-2.5 py-1 text-xs font-bold text-white">{teacher.title} · {teacher.rating}</span>{teacher.specialties.slice(0,2).map((item) => <span key={item} className="rounded-full bg-neutral-100 px-2.5 py-1 text-xs font-semibold">{item}</span>)}</div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><Detail label="Formato" value={formatLabel(teacher.format)}/><Detail label="Idiomas" value={teacher.languages.join(", ")}/><Detail label="Valor demonstrativo" value={currency.format(teacher.price)}/><Detail label="Duração" value={`${teacher.durationMinutes} min`}/></dl><p className="mt-4 flex items-center gap-2 text-sm"><Star size={16} fill="currentColor"/>{teacher.reviewScore.toFixed(1)} · {teacher.reviewCount} avaliações simuladas</p><p className="mt-2 flex items-center gap-2 text-sm text-muted"><CalendarDays size={16}/>{slot ? `Próximo: ${new Date(`${slot.date}T12:00:00`).toLocaleDateString("pt-BR")} às ${slot.time}` : "Sem horário disponível"}</p><div className="mt-auto flex flex-col gap-2 pt-5 sm:flex-row"><button type="button" onClick={onProfile} className="min-h-11 flex-1 rounded-xl border border-line px-3 text-sm font-semibold focus-visible:outline-2 focus-visible:outline-focus">Ver perfil</button><button type="button" onClick={onBooking} disabled={!slot || teacher.status === "indisponível"} className="inline-flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-neutral-950 px-3 text-sm font-semibold text-white disabled:opacity-45"><Clock size={16}/>Consultar horários</button></div><p className="mt-3 text-xs text-muted">Professor e valores fictícios.</p></article>; }
function Detail({ label, value }: { label: string; value: string }) { return <div><dt className="text-xs text-muted">{label}</dt><dd className="mt-1 font-semibold">{value}</dd></div>; }
