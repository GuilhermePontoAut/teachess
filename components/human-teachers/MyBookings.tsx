import { CalendarCheck, RotateCcw, XCircle } from "lucide-react";
import type { LessonBooking } from "@/lib/types/humanTeachers";
import { currency, formatLabel } from "./humanTeachers";
export function MyBookings({ bookings, onCancel, onRepeat }: { bookings: LessonBooking[]; onCancel: (booking: LessonBooking) => void; onRepeat: (booking: LessonBooking) => void }) { if (!bookings.length) return <section className="rounded-2xl border border-dashed border-line-strong bg-white px-6 py-12 text-center"><CalendarCheck className="mx-auto text-neutral-400" size={36}/><h2 className="mt-4 text-lg font-semibold">Nenhum agendamento demonstrativo</h2><p className="mt-2 text-sm text-muted">Os agendamentos confirmados localmente aparecerão aqui.</p></section>; return <div className="grid gap-4 md:grid-cols-2">{bookings.map((booking) => <article key={booking.id} className="rounded-2xl border border-line bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-2"><h2 className="font-semibold">{booking.teacherName}</h2><span className="rounded-full bg-neutral-100 px-2.5 py-1 text-xs font-bold">{booking.status}</span></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><Detail label="Data" value={new Date(`${booking.date}T12:00:00`).toLocaleDateString("pt-BR")}/><Detail label="Horário" value={booking.time}/><Detail label="Formato" value={formatLabel(booking.format)}/><Detail label="Duração" value={`${booking.durationMinutes} min`}/><Detail label="Preço" value={currency.format(booking.price)}/><Detail label="Tema" value={booking.topic || "Não informado"}/></dl>{booking.notes && <p className="mt-3 rounded-xl bg-neutral-100 p-3 text-sm"><strong>Observação:</strong> {booking.notes}</p>}<div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={() => onRepeat(booking)} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-line px-3 text-sm font-semibold"><RotateCcw size={16}/>Repetir agendamento</button>{booking.status !== "Cancelado localmente" && <button type="button" onClick={() => onCancel(booking)} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-red-200 px-3 text-sm font-semibold text-red-700"><XCircle size={16}/>Cancelar demonstração</button>}</div></article>)}</div>; }
function Detail({ label, value }: { label: string; value: string }) { return <div><dt className="text-xs text-muted">{label}</dt><dd className="mt-1 font-semibold">{value}</dd></div>; }
