import { Search, SlidersHorizontal, X } from "lucide-react";
import { teacherSpecialties } from "@/lib/data/humanTeachers";
import type { TeacherFilterState } from "./humanTeachers";
const selectClass = "min-h-11 w-full rounded-xl border border-line bg-white px-3 text-sm focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-focus";
export function TeacherFilters({ filters, onChange, onClear }: { filters: TeacherFilterState; onChange: (value: TeacherFilterState) => void; onClear: () => void }) { const update = <K extends keyof TeacherFilterState>(key: K, value: TeacherFilterState[K]) => onChange({ ...filters, [key]: value }); return <section aria-labelledby="teacher-filters-title" className="rounded-2xl border border-line bg-white p-4 shadow-sm"><div className="flex items-center gap-2"><SlidersHorizontal size={18}/><h2 id="teacher-filters-title" className="font-semibold">Buscar e filtrar</h2></div><label className="mt-4 block"><span className="mb-1.5 block text-sm font-semibold">Nome, especialidade, região ou idioma</span><span className="relative block"><Search className="absolute left-3 top-3.5 text-muted" size={17}/><input type="search" value={filters.query} onChange={(event) => update("query", event.target.value)} className={`${selectClass} pl-10`} placeholder="Ex.: tática ou Recife"/></span></label><div className="mt-4 grid gap-4 sm:grid-cols-2 xl:grid-cols-1"><Select label="Especialidade" value={filters.specialty} onChange={(value) => update("specialty", value)} options={["all", ...teacherSpecialties]} labels={["Todas", ...teacherSpecialties]}/><Select label="Faixa de preço" value={filters.price} onChange={(value) => update("price", value as TeacherFilterState["price"])} options={["all","under-75","76-120","121-200","over-200"]} labels={["Todas","Até R$ 75","R$ 76 a R$ 120","R$ 121 a R$ 200","Acima de R$ 200"]}/><Select label="Disponibilidade" value={filters.availability} onChange={(value) => update("availability", value as TeacherFilterState["availability"])} options={["all","today","three-days","week"]} labels={["Todos","Hoje","Próximos 3 dias","Próxima semana"]}/><Select label="Título enxadrístico" value={filters.title} onChange={(value) => update("title", value as TeacherFilterState["title"])} options={["all","none","cm","fm","im","gm"]} labels={["Todos","Sem título","CM/WCM","FM/WFM","IM/WIM","GM/WGM"]}/><Select label="Ordenar" value={filters.sort} onChange={(value) => update("sort", value as TeacherFilterState["sort"])} options={["recommended","price-asc","price-desc","reviews","experience","availability"]} labels={["Recomendados","Menor preço","Maior preço","Melhor avaliação","Maior experiência","Disponibilidade mais próxima"]}/></div><button type="button" onClick={onClear} className="mt-4 inline-flex min-h-11 w-full items-center justify-center gap-2 rounded-xl border border-line text-sm font-semibold hover:bg-neutral-100 focus-visible:outline-2 focus-visible:outline-focus"><X size={16}/>Limpar filtros</button></section>; }
function Select({ label, value, options, labels, onChange }: { label: string; value: string; options: string[]; labels: string[]; onChange: (value: string) => void }) { return <label><span className="mb-1.5 block text-sm font-semibold">{label}</span><select value={value} onChange={(event) => onChange(event.target.value)} className={selectClass}>{options.map((option, index) => <option key={option} value={option}>{labels[index]}</option>)}</select></label>; }
