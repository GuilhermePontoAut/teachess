"use client";
import { Check, Clipboard, Heart, RefreshCcw, RotateCw } from "lucide-react";

export function PositionStudyTools({ favorite, notes, copied, onFlip, onReset, onCopy, onFavorite, onNotesChange }: { favorite: boolean; notes: string; copied: boolean; onFlip: () => void; onReset: () => void; onCopy: () => void; onFavorite: () => void; onNotesChange: (notes: string) => void }) { const button = "inline-flex items-center justify-center gap-2 rounded-xl border border-line px-3 py-2.5 text-sm font-semibold hover:bg-neutral-100"; return <section className="rounded-2xl border border-line bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold">Ferramentas de estudo</h2><div className="mt-4 grid gap-2 sm:grid-cols-2"><button type="button" onClick={onFlip} className={button}><RotateCw size={16}/>Inverter tabuleiro</button><button type="button" onClick={onReset} className={button}><RefreshCcw size={16}/>Restaurar posição</button><button type="button" onClick={onCopy} className={button}>{copied ? <Check size={16}/> : <Clipboard size={16}/>} {copied ? "FEN copiado" : "Copiar FEN demonstrativo"}</button><button type="button" onClick={onFavorite} aria-pressed={favorite} className={`${button} ${favorite ? "bg-neutral-950 text-white hover:bg-neutral-800" : ""}`}><Heart size={16} fill={favorite ? "currentColor" : "none"}/>{favorite ? "Posição favorita" : "Marcar como favorita"}</button></div><label htmlFor="study-notes" className="mt-5 block text-sm font-semibold">Anotação pessoal</label><textarea id="study-notes" rows={5} value={notes} onChange={(event) => onNotesChange(event.target.value)} placeholder="Registre planos, dúvidas e ideias para revisar depois." className="mt-2 w-full resize-y rounded-xl border border-line p-3 text-sm outline-none focus:ring-2 focus:ring-neutral-300"/><p className="mt-2 text-xs text-muted">Salva localmente e visível apenas para o usuário atual neste protótipo.</p></section>; }
