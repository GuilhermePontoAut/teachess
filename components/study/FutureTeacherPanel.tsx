import { Bot, CalendarDays, UserRound } from "lucide-react";

const disabledButton = "rounded-xl border border-line px-3 py-2.5 text-left text-sm font-semibold disabled:cursor-not-allowed disabled:opacity-50";
export function FutureTeacherPanel() { return <div className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border border-line bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><Bot size={22}/><h2 className="text-lg font-semibold">Professor IA</h2><span className="ml-auto rounded-full bg-neutral-100 px-2.5 py-1 text-xs font-bold uppercase">Em breve</span></div><p className="mt-3 text-sm text-muted">Nenhum modelo de IA está integrado nesta versão.</p><div className="mt-4 grid gap-2">{["Perguntar ao professor IA", "Explique esta posição", "Qual plano devo seguir?", "Quais são as ameaças?"].map((label) => <button key={label} type="button" disabled className={disabledButton}>{label}</button>)}</div></section><section className="rounded-2xl border border-line bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><UserRound size={22}/><h2 className="text-lg font-semibold">Professor humano</h2><span className="ml-auto rounded-full bg-neutral-100 px-2.5 py-1 text-xs font-bold uppercase">Protótipo</span></div><p className="mt-3 text-sm text-muted">A posição permanece privada. Nenhum link público, revisão ou agendamento real é criado.</p><div className="mt-4 grid gap-2">{["Compartilhar com professor", "Solicitar revisão", "Agendar aula"].map((label) => <button key={label} type="button" disabled className={disabledButton}>{label === "Agendar aula" && <CalendarDays className="mr-2 inline" size={15}/>} {label}</button>)}</div></section></div>; }
