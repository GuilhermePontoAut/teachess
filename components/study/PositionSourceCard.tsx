"use client";
/* eslint-disable @next/next/no-img-element -- a origem é um object URL temporário de sessão. */
import { ImageOff, Lock } from "lucide-react";
import type { UploadedPosition } from "@/lib/types/chess";

export function PositionSourceCard({ upload, previewUrl }: { upload: UploadedPosition; previewUrl: string | null }) { return <section className="rounded-2xl border border-line bg-white p-5 shadow-sm"><div className="flex items-center justify-between gap-3"><h2 className="text-lg font-semibold">Imagem de referência</h2><span className="inline-flex items-center gap-1 rounded-full bg-neutral-100 px-2.5 py-1 text-xs font-semibold"><Lock size={12}/>Privada</span></div>{previewUrl ? <div className="mt-4 overflow-hidden rounded-xl bg-neutral-100"><img src={previewUrl} alt={`Imagem de referência de ${upload.title}`} className="max-h-[32rem] w-full object-contain"/></div> : <div className="mt-4 flex min-h-56 flex-col items-center justify-center rounded-xl border border-dashed border-line-strong bg-neutral-50 p-6 text-center"><ImageOff className="text-neutral-400" size={32}/><p className="mt-3 max-w-md text-sm leading-6 text-muted">A imagem original não está disponível após recarregar, pois este protótipo armazena somente os metadados.</p></div>}<p className="mt-3 truncate text-xs text-muted">{upload.fileName} · {(upload.fileSize / 1024 / 1024).toFixed(2)} MB</p></section>; }
